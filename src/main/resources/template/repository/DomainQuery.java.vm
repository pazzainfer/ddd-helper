package ${config.basePackage}#if("$!{config.moduleName}"!="").${config.moduleName}#end.query;

import com.changjinglu.common.mybatis.BaseQuery;
import com.changjinglu.common.mybatis.QueryField;
import com.changjinglu.common.mybatis.QueryType;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.Accessors;

import java.util.Date;

/**
 * ${table.tableComment}查询对象
 * @author ${config.author}
 * @since ${config.dateStr}
 */
@Data
@EqualsAndHashCode(callSuper = false)
@Accessors(chain = true)
public class ${table.entityClass}Query extends BaseQuery {
    /**
     * 主键ID
     */
    private Long id;
}
